log_format combined_extended '$remote_addr - $remote_user [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent" $host:$server_port $request_time $sent_http_x_request_id';